# KataGo 与精简界面验收

日期：2026-09-04

## 已交付

- 删除主对话区、复盘页的重复免责声明；旧记录仅调整显示，不改原始说明。
- 落子标题保留棋色、手数、坐标；完整模型、来源、重试次数仍保留在记录与日志。
- 对话卡片按视口实际宽度重新计算换行和高度；修复 9 路大棋子遮挡坐标。
- 随 Windows 免安装版内置 KataGo 1.18.1 Eigen CPU 与 b18c384 模型。
- 双方停手后异步判死子、计算面积，并与 GTP final_score 交叉核对；一致才自动提交胜负。
- 不一致、失败、超时保留待结算，允许重试、手动核对或继续对弈。
- 悔棋、新开局、关闭窗口取消旧判定；引擎版本、模型、分数、死子数、耗时与一致性写入日志。

## 验证结果

`mvnw.cmd --offline --no-transfer-progress package`：111 项测试通过，0 失败、0 错误、0 跳过。

真实 KataGo 进程测试（非模拟）：

| 输入 | 引擎结果 | 死子 | 面积复核 |
|---|---|---:|---|
| 原 127 手 9 路对局（只读） | B+35.5 | 0 | 黑 62，白 26.5，一致 |
| 9 路单眼死子测试局面 | B+73.5 | 1 | 黑 81，白 7.5，一致 |
| 19 路单眼死子测试局面 | B+353.5 | 1 | 黑 361，白 7.5，一致 |

自动化覆盖：GTP 多行/空响应、编号不匹配、错误响应、退出、超时、取消；坐标方向与跳过 I、死子整块校验、分数范围；自动保存及 SGF 来源、分歧不误判、失败可重试、新局拒绝旧结果；240/300/380 像素视口换行。

另完成真实 EXE 启动与旧棋谱逐手复盘检查。组件渲染图在 `target/engine-preview/`。

## 复现

```powershell
.\mvnw.cmd --offline --no-transfer-progress test
java '-Dstdout.encoding=UTF-8' -cp 'target\classes;target\test-classes' com.yiyan.go.engine.KataGoSmoke '完整对局 XML 路径'
```

KataGo 模型 SHA-256：`9d7a6afed8ff5b74894727e156f04f0cd36060a24824892008fbb6e0cba51f1d`。

KataGo 仅担任终局引擎，未替换 DeepSeek/本地对手的落子策略。程序不会修改既有已结算棋谱的结果。
